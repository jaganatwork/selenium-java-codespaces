{
  "name": "Selenium Java",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "java -version && mvn -v && google-chrome --version && firefox --version",
  "customizations": {
    "vscode": {
      "extensions": [
        "vscjava.vscode-java-pack",
        "vscjava.vscode-maven"
      ]
    }
  }
}